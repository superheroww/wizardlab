# Database schema references

`tables_schema.sql` is the human-friendly, manually maintained reference for the key tables wizardLab relies on.
It is NOT auto-synced, so update it manually whenever the corresponding table structure changes in Supabase.
